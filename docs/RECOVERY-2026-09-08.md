# Nixon Brasil — recuperação seletiva de escopo (2026-09-08)

Origem: PR #19, head `7dbe3b304149427c0c1286eeacba05319aa1fab3`.
Base da recuperação: `main` em `9ab69ca4c8eaf7ab7b6124c022369dfb34eb45b5`.

## Diagnóstico

A PR #19 foi fechada como substituída pelas PRs #20–#26. A decomposição preservou infraestrutura, CMS institucional e cinco revisões de artigos, mas deixou fora da `main` cinco coleções editoriais completas: cronologia, discursos, pessoas, temas e galerias.

Os arquivos dessas coleções continuam no histórico Git. Esta branch os recupera usando os blobs originais da PR #19, sem reescrita.

O primeiro commit desta recuperação também restaurou as antigas rotas de leitura para testar compatibilidade. O Astro compilou as 36 páginas com **0 erros, 0 warnings e 0 hints**, mas `scripts/verify-site.mjs` bloqueou deliberadamente a publicação de `cronologia`, `discursos`, `pessoas`, `temas` e `galerias` enquanto o projeto as considerar seções com acervo insuficiente. O gate foi preservado: as rotas voltaram a ficar inativas no commit seguinte.

No segundo commit, o workflow completo passou: instalação de dependências, `npm audit` sem vulnerabilidades, `astro check`, `astro build` e `scripts/verify-site.mjs`. O job de deploy foi corretamente ignorado por se tratar de pull request.

## KEEP — recuperado na branch

- `cronologia`: 10 marcos documentais, todos com fonte de referência;
- `discursos`: 3 entradas com contexto, fonte original e situação de tradução;
- `pessoas`: 4 perfis ligados ao arquivo;
- `temas`: 3 dossiês temáticos;
- `galerias`: 1 ensaio visual documentado;
- schemas tipados dessas coleções;
- campos opcionais `people` e `themes` em documentos para relações futuras;
- `EditorialEntry.astro`, preservado como componente de futura reativação das rotas.

## ROTAS PRESERVADAS NO HISTÓRICO, NÃO PUBLICADAS

O commit `48bd6e4be197610b6630f3fdb9ce8d762b476187` recuperou e comprovou a compilação das rotas originais da PR #19. Elas permanecem recuperáveis pelo Git, mas não ficam na árvore final desta branch porque o verificador editorial atual exige que essas seções continuem fora da publicação.

Não alterar `scripts/verify-site.mjs` apenas para tornar esta recuperação verde.

## PRESERVAR DA MAIN

- identidade pública `nixonbrazil`;
- artigos revisados pelas PRs #22–#26;
- páginas institucionais atuais;
- `src/data/site.json` e o fluxo atual da homepage/menu;
- arquitetura atual do acervo;
- feed, assinatura, segurança, build e deploy atuais.

## NÃO RECUPERAR AUTOMATICAMENTE

- antigo `src/data/settings/*`;
- antigo Header/Footer/BaseLayout;
- antigo `global.css` completo;
- README e manuais antigos;
- workflow/Dependabot da PR #19;
- página de vídeos;
- qualquer renomeação para “Arquivo Nixon” ou “Cadernos 37”.

## RESEARCH-NEEDED / SEGUNDO GATE

Antes de reativar qualquer seção pública:

1. auditar cada entrada histórica contra a fonte indicada;
2. definir um limiar editorial objetivo de densidade para cada coleção;
3. ampliar as coleções que não atingirem esse limiar;
4. integrar apenas então as coleções aprovadas ao Pages CMS atual;
5. reativar suas rotas, adicionar ao sitemap e fazer revisão visual desktop/mobile;
6. decidir se a seção entra no menu principal ou permanece como índice secundário;
7. atualizar o verificador somente como consequência dessa decisão editorial, não como atalho para passar no CI.

## Critério de merge futuro

Nenhuma mudança desta branch deve chegar à `main` sem:

- build verde;
- revisão do diff;
- ausência de regressão nos artigos e páginas atuais;
- auditoria factual do conteúdo recuperado;
- decisão editorial explícita sobre quais coleções devem voltar a ser públicas.
